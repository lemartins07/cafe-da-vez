# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_ENV=local

# Local database. Vercel will use Supabase connection strings instead.
DATABASE_URL=postgresql://cafe_da_vez:cafe_da_vez@localhost:5432/cafe_da_vez?schema=public
DIRECT_URL=postgresql://cafe_da_vez:cafe_da_vez@localhost:5432/cafe_da_vez?schema=public

# Supabase Auth (configured in Phase 3)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Somente servidor. Necessária para redefinição administrativa de senhas.
SUPABASE_SECRET_KEY=
