# Features

As funcionalidades do Café da Vez serão organizadas por domínio nesta pasta.
Os primeiros módulos previstos são `auth`, `members` e `rotations`.

Cada feature poderá conter seus componentes, validações, ações de servidor e
testes. Código realmente compartilhado permanece em `src/components` ou
`src/lib`.
